# CSD5000 双端口隔离配置脚本使用手册

**脚本名称：** csd5000.sh  
**适用硬件：** ScaleFlux CSD5000 + AIC F2026-01-G5 JBOF（U.2 双端口 NVMe）  
**配置模式：** 隔离模式（Separate），非共享模式  
**文档版本：** v1.0  
**更新日期：** 2026-08-13  

---

## 一、脚本做什么

`csd5000.sh` 用于在 **同一 JBOF 机箱的两个控制器节点** 之间，批量或单盘配置 ScaleFlux 盘的 **Namespace（命名空间）**，实现以下三种策略：

| 策略 | 命令 | 效果（以 7.68TB 盘为例） |
|------|------|--------------------------|
| 对半独享（50/50） | split-all / split-one | Port0 得 3.84TB，Port1 得 3.84TB，互不可见 |
| 整盘仅 Port0 | own-p0 | Port0 得 7.68TB，Port1 无块设备 |
| 整盘仅 Port1 | own-p1 | Port1 得 7.68TB，Port0 无块设备 |

**重要说明：**

- 脚本操作的是 **NVMe Namespace 层**，会 **删除已有 namespace 及数据**。
- Namespace 配置完成后，还需在 **各节点分别格式化（mkfs）和挂载（mount）** 才能给业务使用。
- **隔离模式下**，Port0 节点 **看不到** Port1 的块设备，反之亦然。这是正常现象。

---

## 二、架构说明

### 2.1 JBOF 双控制器拓扑

一台 F2026-01-G5 JBOF 机箱包含 **两个控制器节点**：

```
┌─────────────── 同一 JBOF 机箱 ───────────────┐
│                                              │
│   Port0 节点 (--port0)    Port1 节点 (--port1) │
│   通常为 Port #0           通常为 Port #1      │
│   cntlid = 0               cntlid = 0x3f      │
│                                              │
│   ┌─ NS1 (3.84TB) ─┐   ┌─ NS2 (3.84TB) ─┐   │
│   │  仅本节点可见   │   │  仅本节点可见   │   │
│   └────────────────┘   └────────────────┘   │
│              └──── 同一块物理盘 ────┘         │
└──────────────────────────────────────────────┘
```

### 2.2 本环境节点配对（示例）

| JBOF 机箱 | Port0（--port0） | Port1（--port1） |
|-----------|------------------|------------------|
| JBOF #1 | 192.168.255.95 (node1) | 192.168.255.97 (node3) |
| JBOF #2 | 192.168.255.96 (node2) | 192.168.255.98 (node4) |

**注意：** 必须配对 **同一机箱** 的两个节点。  
例如 JBOF#2 应使用 `.96 + .98`，**不能** 使用 `.96 + .95`（不同机箱，脚本会报「未发现配对盘」）。

---

## 三、使用前准备

### 3.1 运行位置

在 **Port0 节点** 上运行脚本（或任意能 SSH 到 Port0、Port1 的管理机）。

推荐：在 Port0 节点本机运行，例如 JBOF#2 在 **192.168.255.96** 上执行。

### 3.2 依赖检查

两台节点均需满足：

- 已安装 `nvme-cli`（命令 `nvme` 可用）
- 已安装 `python3`
- ScaleFlux 盘可被 `lspci -d cc53:` 识别
- 双端口链路正常：`Speed 32GT/s, Width x2`

### 3.3 配置 Port0 → Port1 免密 SSH（首次必做）

在 **Port0 节点** 上执行：

```bash
ssh-keygen -t rsa -N "" -f ~/.ssh/id_rsa
ssh-copy-id root@<Port1的IP>
```

示例（JBOF#2）：

```bash
ssh-copy-id root@192.168.255.98
```

验证：

```bash
ssh root@192.168.255.98 hostname
# 应返回 node4，且无需输入密码
```

### 3.4 安装脚本

```bash
chmod +x /root/csd5000.sh
```

---

## 四、命令速查

### 4.1 命令一览

| 命令 | 是否改数据 | 说明 |
|------|------------|------|
| list | 否（只读） | 列出所有可操作的配对盘及当前状态 |
| split-all | 是 | 全部配对盘设为 50/50 对半独享 |
| split-one | 是 | 指定单盘设为 50/50 对半独享 |
| own-p0 | 是 | 指定单盘整盘仅给 Port0 |
| own-p1 | 是 | 指定单盘整盘仅给 Port1 |

### 4.2 参数说明

| 参数 | 必填 | 说明 |
|------|------|------|
| --port0 IP | 是 | Port0 侧节点 IP（JBOF 控制器 Node1） |
| --port1 IP | 是 | Port1 侧节点 IP（JBOF 控制器 Node2） |
| --sn SN | split-one / own-p0 / own-p1 必填 | 盘序列号，如 SF2451C0598H |
| --confirm | 写操作必填 | 确认执行（会删除 namespace 和数据） |
| --dry-run | 否 | 演练模式：只打印将执行的 nvme 命令，不实际写入 |
| -h / --help | 否 | 显示帮助 |

---

## 五、操作步骤（推荐流程）

### 步骤 1：查看当前盘状态（只读，安全）

```bash
./csd5000.sh list --port0 192.168.255.96 --port1 192.168.255.98
```

**输出字段说明：**

| 字段 | 含义 |
|------|------|
| SN | 盘序列号（唯一标识） |
| P0_CTRL | Port0 节点上该盘对应的控制器设备，如 /dev/nvme0 |
| P1_CTRL | Port1 节点上该盘对应的控制器设备，如 /dev/nvme5 |
| TOTAL | 物理盘总容量（约 7.68TB） |
| P0_NS | Port0 节点上的块设备及容量，如 /dev/nvme0n1(3.84TB) |
| P1_NS | Port1 节点上的块设备及容量（在 .98 上，不在 .96 上） |
| 状态 | 可配置 / 已对半(50/50) / 仅P0有NS / 仅P1有NS |

**状态含义：**

- **可配置**：两侧均无 namespace，可直接配置
- **已对半(50/50)**：两侧各约 3.84TB，配置完成
- **仅P0有NS / 仅P1有NS**：只配了一边，需重新配置或补配

### 步骤 2：演练（可选，推荐）

```bash
./csd5000.sh split-one \
  --port0 192.168.255.96 --port1 192.168.255.98 \
  --sn SF2451C0598H --dry-run
```

演练模式 **不会** 执行任何 nvme 写入，只打印将要运行的命令。

### 步骤 3：正式配置

**方式 A：全部盘对半（批量）**

```bash
./csd5000.sh split-all \
  --port0 192.168.255.96 --port1 192.168.255.98 \
  --confirm
```

**方式 B：单盘对半**

```bash
./csd5000.sh split-one \
  --port0 192.168.255.96 --port1 192.168.255.98 \
  --sn SF2451C0598H --confirm
```

**方式 C：单盘整盘仅 Port0**

```bash
./csd5000.sh own-p0 \
  --port0 192.168.255.96 --port1 192.168.255.98 \
  --sn SF2525C8202H --confirm
```

**方式 D：单盘整盘仅 Port1**

```bash
./csd5000.sh own-p1 \
  --port0 192.168.255.96 --port1 192.168.255.98 \
  --sn SF2539C1626H --confirm
```

### 步骤 4：验收

```bash
# 用 list 查看汇总
./csd5000.sh list --port0 192.168.255.96 --port1 192.168.255.98

# 或在两侧分别查看
nvme list | grep ScaleFlux          # 在 Port0 节点
ssh root@192.168.255.98 'nvme list | grep ScaleFlux'   # 查看 Port1
```

**对半配置成功的标志：**

- list 状态为 **「已对半(50/50)」**
- Port0 节点：每盘约 **3.84TB** 块设备
- Port1 节点：每盘约 **3.84TB** 块设备
- 两侧 SN 相同，设备名（/dev/nvmeXn1）可能不同

### 步骤 5：格式化与挂载（脚本不自动执行）

Namespace 就绪后，**各节点独立操作**：

**在 Port0 节点（.96）：**

```bash
mkfs.xfs /dev/nvme0n1
mkdir -p /data/disk1
mount /dev/nvme0n1 /data/disk1
```

**在 Port1 节点（.98）：**

```bash
mkfs.xfs /dev/nvme0n1
mkdir -p /data/disk1
mount /dev/nvme0n1 /data/disk1
```

如需开机自动挂载，写入 `/etc/fstab`（使用 UUID 或 LABEL，建议不要用裸设备名）。

---

## 六、混合配置示例

可以 **不同盘用不同策略**，例如 JBOF#2 的 13 块盘：

| 盘 SN | 策略 | 命令 |
|-------|------|------|
| SF2525C8202H 等 10 块 | 对半 50/50 | split-one --sn xxx --confirm |
| SF2539C1626H | 整盘仅 Port0 | own-p0 --sn SF2539C1626H --confirm |
| SF2525C8339H | 整盘仅 Port1 | own-p1 --sn SF2525C8339H --confirm |

**同一盘只能选一种策略**，不能既对半又整盘。

---

## 七、常见问题

### Q1：list 显示 P1_NS 全是「-」，是否没配成功？

**不一定。** 早期版本 list 有显示 bug（已修复）。请以以下方式核实：

```bash
ssh root@<Port1_IP> 'nvme list | grep ScaleFlux'
```

若 Port1 上有 3.84TB 块设备，则配置成功。

### Q2：为什么在 Port0 节点 lsblk 看不到 P1 的盘？

**正常现象。** 隔离模式下：

- Port0 节点 **只能看到** Port0 的 namespace
- Port1 节点 **只能看到** Port1 的 namespace

要在 Port1 上看盘，请登录 Port1 节点或使用 SSH。

### Q3：报错「未发现 Port0/Port1 两侧均可见的 ScaleFlux 盘」

**原因：** --port0 和 --port1 不是同一 JBOF 机箱的配对节点。

**解决：** 检查节点配对表（见第二节），确保使用正确 IP 组合。

### Q4：报错 NS_INSUFFICIENT_CAPACITY

**原因：** 对半拆分时，两半 sector 之和超过物理盘总容量。

**说明：** 当前 csd5000.sh 已自动按 tnvmcap 对半计算，正常不应再出现此错误。若仍出现，请检查是否手动改过 sector 参数。

### Q5：apply 之前配置了半成品（只有 P0 有 NS）怎么办？

对单盘重新执行 split-one 即可，脚本会先 delete-ns 再重建：

```bash
./csd5000.sh split-one --port0 ... --port1 ... --sn <SN> --confirm
```

### Q6：cntlid 是什么？需要手动填吗？

不需要。脚本自动读取：

- Port0 侧通常为 **0**
- Port1 侧通常为 **0x3f**

---

## 八、安全注意事项

1. **写操作必须加 --confirm**，否则脚本拒绝执行。
2. **建议先 --dry-run 演练**，确认命令无误后再 --confirm。
3. **执行前停止业务**，卸载已挂载的 ScaleFlux 块设备。
4. **数据不可恢复**：delete-ns 会清除 namespace 内全部数据。
5. **不要在业务运行时** 对正在使用的盘执行 split / own 操作。
6. 批量操作建议 **先用 split-one 测一块盘**，验收 OK 后再 split-all。

---

## 九、命令复制区（JBOF#2 示例）

以下命令可直接复制使用（IP 按实际环境替换）：

```bash
# ========== 只读查看 ==========
./csd5000.sh list --port0 192.168.255.96 --port1 192.168.255.98

# ========== 演练（不改数据） ==========
./csd5000.sh split-one --port0 192.168.255.96 --port1 192.168.255.98 --sn SF2451C0598H --dry-run

# ========== 单盘对半 ==========
./csd5000.sh split-one --port0 192.168.255.96 --port1 192.168.255.98 --sn SF2451C0598H --confirm

# ========== 全部对半 ==========
./csd5000.sh split-all --port0 192.168.255.96 --port1 192.168.255.98 --confirm

# ========== 单盘整盘仅 Port0 ==========
./csd5000.sh own-p0 --port0 192.168.255.96 --port1 192.168.255.98 --sn SF2525C8202H --confirm

# ========== 单盘整盘仅 Port1 ==========
./csd5000.sh own-p1 --port0 192.168.255.96 --port1 192.168.255.98 --sn SF2539C1626H --confirm

# ========== 验收 ==========
./csd5000.sh list --port0 192.168.255.96 --port1 192.168.255.98
ssh root@192.168.255.98 'nvme list | grep ScaleFlux'
```

**JBOF#1 只需替换 IP：**

```bash
--port0 192.168.255.95 --port1 192.168.255.97
```

---

## 十、脚本内部逻辑（供了解）

对半配置（split-one）时，脚本按以下顺序执行：

```
1. 读取该盘 tnvmcap，自动计算：
   half0 = tnvmcap / 512 / 2
   half1 = tnvmcap / 512 - half0

2. Port0 节点：delete-ns → create-ns(half0) → attach-ns -n 1 → reset

3. Port1 节点：delete-ns → create-ns(half1) → attach-ns -n 2 → reset

4. 两侧 nvme list 验收
```

整盘配置（own-p0 / own-p1）只在对应侧 create 一个满容量 namespace。

---

**文档结束**

如有问题，请联系存储运维同事，并提供 `list` 命令完整输出。
